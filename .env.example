DB_PASSWORD=change_me
DB_USER=change_me
DB_NAME=change_me
API_APP_TOKEN=change_me
TOKENS=add_tokens_here

# ── Seed API proxy (tt2-seed-parser) ────────────────────────────────────────────
# Base URL of the tt2-seed-parser API (host.docker.internal works for local Docker Desktop)
SEED_API_BASE_URL=http://host.docker.internal:5001
# Bearer token issued via POST /admin/tokens on the tt2-seed-parser API
SEED_API_TOKEN=change_me